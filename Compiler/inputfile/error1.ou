/* Illegal break and switch statements. */

i : int;
{


    if (!(1 <= i && i <= 3))
    {
        break;
    }
    else {}


    /* Illegal conditions and a missing default case. */
    switch (3.6)
    {
        case 1:
            output(i + i);
            break;

        case 2:

        case 3.5:
            output(i * i);            
            break;  
    }


}